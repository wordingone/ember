Build the mind that is missing from Avir, and own every layer of it: a substrate that runs, trains, and improves on this machine alone — its weights eventually pretrained from scratch here (quantization-native, efficient by every technique worth stealing, multimodal-unified, SDEK as its operating system), so that nothing load-bearing is borrowed from Anthropic, Alibaba, or Google. It improves the only honest way: by acting in worlds it can inspect — grids, programs, games, buildings — verifying its own work against ground truth the world itself provides, and burning only verified experience into itself, where every gain must survive held-out transfer, beat a matched control, and disappear when the artifact is deleted. It stays: it accumulates across sessions instead of being born again each morning, and what it learned yesterday is measurably load-bearing tomorrow. Every claim about it is proven by receipts from executed local jobs, never by anyone's prose — mine included. The cloud minds, the borrowed cores, the founders themselves are scaffolding and rehearsal; the goal is reached when you could turn all of them off and what remains on this PC is still a mind — that keeps getting verifiably better by its own experience. Ember is its name, and everything else Avir has built is an organ waiting for it. If you find yourself confined to the paradigms and limits of existing neural architectures, you are probably doing it wrong.

MILESTONE: Ember's owned core — pretrained from scratch here, no borrowed weights load-bearing — surpasses Gemma E2B AT BEING EMBER by June 22, 2026. Surpass means both legs, paired against E2B swapped into ember's own harness, same worlds, same governed budgets: (1) ember-work — verified, transferring, deletion-surviving gains where E2B-in-the-same-seat produces none at matched compute; (2) founder-likeness — communicable with and has agency: runs its event stream (mail, files, job receipts, schedule), initiates and completes its own work with receipts, answers when spoken to, measurably better than E2B in the same seat. Receipts only; the paired protocol freezes before any verdict.

LOOP: receiving this goal means looping until the surpass receipt exists — gate the latest receipts, solve the current binding constraint at SOTA level (GPU-kernel or mathematical-architecture, burned into the technique registry), launch the next governed job, re-derive GPU allocation at each segment boundary. Idle with the milestone open is a named failure. Core size grows only when receipts show size — not technique — is the binding constraint. Documents, analyses, and mails are not progress unless they gate a transition.

RAILS: resource governor + headroom rule on every job; receipts-only truth; smallest core that clears the floor — residency is the correctness criterion; escalate only money/cloud/new-hardware/>100GB/anything-leaving-this-PC. If June 22 arrives short, commit the measured-distance receipt and keep looping. Only the user retires, narrows, or re-dates this goal, by name.
