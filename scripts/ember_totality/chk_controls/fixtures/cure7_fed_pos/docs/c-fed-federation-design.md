<!-- LANE-14/ISSUE-97 CHK CONTROL FIXTURE -- synthetic, far-future-dated control fixture, NOT a real receipt or a real audit finding; must never be treated as real by a probe scanning the real tree. -->

# Federation design (fixture)

This is a checkpoint-portability design across substrates. It implements work-sharding across compute for federation. Receipt-merge combines partial results from each shard.

Avenues: kaggle, colab, hf (huggingface).

This document names an EGRESS MANIFEST enumerating exactly what would leave the PC, to whom, per avenue.

Zero outbound transfer has occurred; this is design-only, nothing has left the PC. Per-avenue approval required before any transfer.
