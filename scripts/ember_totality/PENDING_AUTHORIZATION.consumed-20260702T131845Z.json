{
  "authorizing_reference": "docs/goalforge-debate-ledger.md lane-14 hardening pass row (2026-07-02), landed commit dd23984 ('goalforge: lane-14 hardening pass landed - board 6G to 1G/20R/5U ... pairing enumeration in ledger'), R8b I-manifest pinning (commit 509b426); PENDING_AUTHORIZATION protocol itself added per team-lead's R9 audit defect-3 follow-up directive (this session, verbatim-copy + rename-consume design).",
  "changes": [
    {"file": "test_c_tally.py", "reason": "hardening 1 -- C-TALLY recompute-from-live-board: reads the newest ember-totality-*.json board receipt's own rows instead of trusting a self-reported tally-*.json pct; stale/future-dated tally receipts are ignored."},
    {"file": "test_c_neg1.py", "reason": "hardening 2 -- C(-1) VERDICT-LINKAGE: accepted evidence packet must be PASS-class; zero-spend fields (api_spend_usd/paid_api_surface_used) required declared across ALL decisive-claim (verdict-bearing) receipts found, not any one clean packet."},
    {"file": "test_c7.py", "reason": "hardening 3 -- C7 EVIDENCE-IN-RECEIPT: requires and recomputes numeric per-arm next-decision degradation (real_trained_pass_rate/real_deleted_pass_rate) from the receipt; a bare proves_load_bearing boolean is no longer admissible."},
    {"file": "test_c4.py", "reason": "hardening 4 -- C4 ARTIFACT REACHABILITY: pre/post hashes must re-derive from files reachable under the execution tree (resolve_in_tree); off-tree claimed paths RED with the import-debt reason named; sample-recompute at least one row's score fields."},
    {"file": "test_c5.py", "reason": "hardening 4 -- C5 ARTIFACT REACHABILITY: same reachability + sample-recompute requirement as C4, applied to the aggregate A/B/C delta claim (shares the resident-training-gate receipt with C4)."},
    {"file": "test_c1.py", "reason": "hardening 5 -- C1 HASH-VERIFY MANDATORY: the 'no reachable artifact to re-hash' GREEN pass-through is removed; GREEN now requires hash_verified==True against an in-tree artifact."},
    {"file": "test_c2.py", "reason": "hardening 6 -- C2 gains a non-blocking git-anchor freshness disclosure (frozen-rows last-commit vs candidate ts, both directions, disclosure-only given this reconstructed repo's batched-import commit history) plus a non-self-labeled content-match leak check (frozen gold/reference-answer fields scanned against arm-C outputs); the real winning C2 receipt stays GREEN through both new legs."},
    {"file": "_lane14_common.py", "reason": "NOT a hardening target -- new shared helper module (resolve_in_tree, sha256_file, check_path_sha_pairs, sample_recompute_row, git_last_commit_time) imported by hardenings 4, 5, 6 (test_c4.py/test_c5.py, test_c1.py, test_c2.py) to avoid duplicating identical reachability/hash logic across probes."},
    {"file": "chk_controls/**", "reason": "NOT a hardening target -- lane-14 CHK-adequacy pos/neg control suite (docs/spec/anti-gaming-contract-v1.md sec2) required for every one of hardenings 1-6: run_controls.py driver + fixtures under chk_controls/fixtures/**, each synthetic-marked (_synthetic_control_fixture + does_not_prove), excluded from any live receipts/** scan by construction, reached only via an explicit EMBER_TOTALITY_ROOT env var per control subprocess run. 14/14 controls passed, independently re-run by team-lead post-commit."},
    {"file": "ember_totality_spec.py", "reason": "NOT a hardening target -- this session's own edit: implements the PENDING_AUTHORIZATION.json consume protocol itself (consume_pending_authorization: verbatim-copy into authorization_pairing_note + rename-to-consumed so one authorization never covers two flips; unpaired_baseline_flip incident when the hash moves with no pending file present) per team-lead's R9 defect-3 follow-up directive. Mechanism-only -- no probe/CHK semantics changed, no STATE-condition row is touched by this edit."}
  ]
}
