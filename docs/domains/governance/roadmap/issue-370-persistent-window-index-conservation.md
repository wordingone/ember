# Issue #370 persistent window-index conservation

Status: `SUPERSEDED_NOT_PLANNED` only after this exact carrier receives an
independent exact-head PASS, fresh required checks are green, and the carrier
merges. This document does not claim that a current index or overlap run
executed.

## Historical subject retired

Issue #370's old W1/shards-v0 launch vehicle is unavailable historical
provenance only. The checked TOKEN-SHARDS-V0 receipt binds the exact 26
historical names and hashes, while `v0-00025.bin` is absent from current public
custody. Substituted, regenerated, partial, or merely similar shards cannot
prove the historical index result.

Historical PRs #374 and #553 closed unmerged. The observed roughly 25-minute
CPU recheck gating roughly seven minutes of training and #359's identical
batch+receipt+code cache remain provenance, not a current performance result.
GPU acceleration remains rejected unless the persistent-index route is first
independently falsified.

## Accepted current owners

- OPEN EMBER-02/#1116 accepts the persistent-index science, exact scanner
  equivalence, invalidation, fallback, and path-disclosure contract:
  https://github.com/wordingone/ember/issues/1116#issuecomment-5225249707
- OPEN Ember Lab/#898 accepts only operational overlap/resource/receipt
  custody and the step-zero refusal fence:
  https://github.com/wordingone/ember/issues/898#issuecomment-5225249951
- #370 bidirectional bridge:
  https://github.com/wordingone/ember/issues/370#issuecomment-5225251282

The exact reviewed transfer packet behind both owner acceptances is
`E234B015B04208B7EEF670E7B54805D7B27F1604FA38FD73FB12980165D4014A`
(4,699 bytes), independently reviewed with no P0/P1. The packet is evidence
provenance only; it grants no execution or completion credit.

The owners are independent and complementary. Neither inherits evidence from
the other, and neither is closed by this carrier. #440 remains a separate open
owner for its exact-50-token heldout predicate and shards-v1 contract; no #370
cache or index borrows #440 completion evidence.

## Persistent 13-gram/window index contract

#1116 preserves the historical requirement for one content-addressed
persistent 13-gram/window index over the exact 26-shard TOKEN-SHARDS-V0
manifest, including combined-SHA invalidation. Because the exact historical
bytes are absent, that result remains unavailable.

Any current execution builds the same index contract over the exact complete
admitted current corpus and binds it to the complete corpus-manifest combined
SHA. Before selecting a cure, the execution preregisters and measures build
time, index bytes, format/version, load time, and query time. Any corpus,
format, version, or hash mismatch invalidates the index.

The index must produce exact-result equivalence against the existing full
scanner, including all current contamination predicate semantics. Missing,
SHA-mismatched, corrupt, partial, foreign, or stale index state fails closed
to a disclosed full scan; it never silently skips work or accepts a corpus.
Every receipt distinguishes exactly `index-hit`, `full-scan`, and the existing
`cache-hit` path.

## Current Ember Lab overlap and refusal contract

#898 may permit CPU recheck or index load to overlap model, optimizer, and
checkpoint loading. The first optimizer or training step is nevertheless
hard-fenced behind a clean contamination verdict.

A contaminated, missing, malformed, foreign, stale, interrupted, or failed
verdict refuses and tears down before step one. Evidence binds process,
job/lease, source/config/checkpoint, corpus/index, phase, timing/resource,
verdict, teardown, and receipt identity. Receipt-first interruption and
rollback preserve the last truthful path.

This contract creates no launcher, supervisor, index authority,
contamination predicate, receipt family, or deprecated historical daemon
path. Current Ember Lab/governed runner/custody remains the sole operational
authority.

## Negatives and rollback

- No absent historical shard, prose assertion, synthetic fixture, helper, or
  current-but-different corpus can prove the historical result.
- No stale or mismatched index may be silently reused or treated as a hit.
- No cache or index result may inherit #440 predicate completion.
- No optimizer step may occur before a clean verdict, including after restart
  or interruption.
- Refusal and teardown evidence is written before abort; foreign or partial
  evidence cannot authorize continuation.
- Rollback is ordinary Git revert of this carrier plus reopening #370 while
  the accepted owner comments remain append-only custody.

## Terminal falsifier and reopen rule

Scientific completion requires current master to independently replay:

1. a content-addressed persistent 13-gram/window index bound to the exact
   complete admitted corpus and invalidated by any combined-SHA mismatch;
2. measured build/size/load/query receipts;
3. exact scanner equivalence plus invalidation, corruption, partial, and
   foreign negatives;
4. explicit `index-hit`, `full-scan`, and `cache-hit` receipt paths; and
5. a fail-closed overlap test proving no optimizer step starts before a clean
   verdict and teardown occurs on refusal.

This carrier instead permits only `SUPERSEDED_NOT_PLANNED`; every scientific
and operational falsifier stays open on #1116/#898. Loss or narrowing of an
accepted owner link, exact historical custody statement, 13-gram/window or
combined-SHA requirement, measurement, equivalence, fallback/path disclosure,
#440 separation, step-zero fence, negative, rollback, or authority boundary
reopens #370.

## Credit and authority boundary

- `completion_credit=false`
- `scientific_execution_credit=false`
- `acquisition_credit=false`
- `result_credit=false`
- `gpu_credit=false`
- `training_credit=false`
- `checkpoint_credit=false`
- `capability_credit=false`
- `milestone_credit=false`

Current EMBER-02/#1116 and Ember Lab/#898 remain the sole authorities in their
accepted scopes.

`NO_NEW_PARALLEL_AUTHORITY`
