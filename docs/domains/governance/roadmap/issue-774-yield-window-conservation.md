# Issue #774 terminal conservation ruling

Status: `EXPIRED_SUPERSEDED_NOT_PLANNED` for the dated 2026-07-11 GPU
yield-window runbook. Reusable lifecycle obligations are conserved by current
Ember Lab dispatch/lease authority and issue #802; the scheduled science is
owned by its surviving canonical experiment carriers.

Source master: `4d814bbec3116ea0eb9d16298a7747c043828727`.

## Historical scope and outcome boundary

Issue #774 was a one-window coordination object, not a standing experiment. It
scheduled a borrowed inference-tenant stop, a historical 2.2B #702 attribution
rerun, #764 434M/2.2B micro-bench legs, a #449 rider, and a health-checked
restore. Its lease tokens, initial deadline, and extensions all expired on
2026-07-11.

The public comments record real incident lessons: required measurements must be
fresh at window-open; a missing independent reviewer cannot silently become an
undisclosed clearance; measurements must survive schema rejection; a receipt
schema must be probed before an expensive launch; invalid bytes are quarantined
rather than deleted; consumers must not read a stale prior receipt while a
quarantine sibling exists; and restore must be health checked and receipted.

The window did not complete its frozen sequence. Two #702 attempts used the
wrong subject or lost their output; attempt 3 later produced only the bounded
historical result conserved by `issue-702-offload-attribution-conservation.md`.
#764 did not complete its full CPU/GPU/equivalence program and PR #1511 closed
without merge, as conserved by `issue-764-factor1-microbench-conservation.md`.
The scheduled #449 scientific question is not completed by a runbook entry.

No lease completion, restored-tenant continuity, #449 result, #702 current
attribution, #764 result, GPU result, training result, checkpoint result,
capability result, or milestone completion is claimed by this ruling.

## Why the runbook cannot remain current

The runbook's calendar window and tokens are expired. Its scheduled 434M and
2.2B subjects are below the current 3B genesis floor and are read-only history.
Its borrowed `llama-server`/Qwen tenant is not the current owned Ember serving
authority, and the former caller-written token shape is not the current lease
contract.

Current Ember Lab already owns dispatch identity, resource leases, fenced job
lifecycle, host/resource preflight, content-addressed operational receipts, and
recovery. Issue #802 owns the current owned-server supervision contract:
health-loop cadence, planned-outage ordering, identity-safe restart, headroom,
receipt/activity evidence, bounded backoff, alarm, and no foreign-process kill.
That lifecycle transfer is accepted append-only at
https://github.com/wordingone/ember/issues/802#issuecomment-5220810202.
The historical experiments have current owners: #702 and #764 have transferred
their surviving current-scale obligations to #707 plus EMBER-02/EMBER-05. #449
remains a separate scientific issue and receives no result credit here.

Reopening the expired clock, old token files, borrowed tenant, or sub-3B commands
would create a second lifecycle authority and violate the active architecture.
This is supersession of an execution schedule, not evidence that its scheduled
experiments succeeded.

## Lossless obligation transfer

The reusable #774 obligations map as follows:

- Ember Lab remains the sole owner of atomic lease acquisition, epoch and job
  identity, dispatch-manifest validation, exact program/source bindings,
  process containment, termination, release, recovery, and content-addressed
  terminal receipts;
- current host, storage, commit, VRAM, process-identity, and tenant checks are
  remeasured by the governed admission path before spawn and at required phase
  boundaries; historical preflight values and caller booleans cannot authorize;
- issue #802 owns current owned-server planned-outage semantics, health probes,
  restart eligibility, identity fencing, headroom, restore cost/death cause,
  activity evidence, restart-rate backoff, alarms, and release on every exit;
- receipt producers validate their schema before expensive execution; runtime
  validation failure preserves exact candidate bytes in non-selectable
  quarantine, never deletes the sole measurement or leaves a stale success path
  consumable as current evidence;
- issue #707 plus EMBER-02/EMBER-05 retain the current-scale optimizer,
  attribution, cure, resource, equivalence, quality, and rollback obligations
  previously scheduled as #702/#764 legs; #707 accepts this exact #774
  conservation boundary at
  https://github.com/wordingone/ember/issues/707#issuecomment-5220810089;
- issue #449 retains its own scientific hypothesis until separately completed,
  superseded, or conserved. #774 supplied only a time slot and cannot discharge
  that science;
- every current GPU or CPU experiment obtains a fresh current-authority route;
  no expired token, dated outage decision, historical daemon, cbase launcher,
  borrowed model server, or parallel receipt family may be revived.

The current mechanisms and canonical issues own every reusable clause. #774
owned only the expired orchestration window; no unique current obligation
remains after this mapping.

## Closure effect

Close #774 as not planned/expired-superseded after this ruling is on public
master and its transfers are linked from #802 and #707. Historical comments
remain incident provenance, not current execution authority. This closure does
not close #449, #707, or #802 and does not claim any scheduled leg executed.

`NO_NEW_PARALLEL_AUTHORITY`
