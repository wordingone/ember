# Issue #627 historical-vehicle conservation and current-owner crosswalk

Status: `SUPERSEDED_NOT_PLANNED` is proposed for the stale v9/C8/sub-3B
execution vehicle only. This document does not close #627, claim a result, or
authorize a run; the public issue remains open until its disposition is made
from this lossless transfer.

Source master for this crosswalk: `2401f27a2056ae639dcb5e8f09d8ad903d35c2a2`.

## Retired vehicle and evidence boundary

Issue #627 described a v9 cure on the historical sub-3B path: R2 in-process
commit reads, R3 persistent shadow gradients/direct copies, falsifiable
counters, an in-run pace gate, a checkpointed block-04/step-806 continuation,
and subsequent CPU/GPU/equivalence evidence. The named v9/C8/2.2B subject,
block-04/step-806 continuation, historical launcher, and historical receipt
authority are not admissible current Ember work. They are retained as
provenance and are not resurrected or relabeled as current 3B evidence.

The public record contains apparatus findings and negative boundaries, not a
closure-grade current run. No optimizer win/loss, governor win, speedup, CUDA
result, training result, checkpoint result, capability result, or milestone
completion is claimed here.

## Lossless transfer to the canonical open owners

The transfer was appended to the live canonical issue threads before this
carrier was prepared:

- [#707 transfer comment](https://github.com/wordingone/ember/issues/707#issuecomment-5221809682)
  owns optimizer and transition obligations.
- [#898 transfer comment](https://github.com/wordingone/ember/issues/898#issuecomment-5221810715)
  owns current dispatch, resource, process, abort, and pace obligations.
- Earlier accepted historical transfers remain orientation evidence:
  [#764 to #707](https://github.com/wordingone/ember/issues/707#issuecomment-5220640967)
  and [#702 to #707](https://github.com/wordingone/ember/issues/707#issuecomment-5220742929).

### #707: optimizer, direct-copy, and equivalence owner

The following clauses survive without scale or authority loss and must be
implemented and evidenced on the current clean-genesis 3B-or-larger subject:

- read commit state in process; no per-step PowerShell or other subprocess;
- keep shadow gradients and direct copies persistent, with no staged copy that
  silently changes the measured mechanism;
- emit falsifiable lazy-create and governor-read counters, including a
  positive control rather than a vacuous zero-spawn assertion;
- cover allocation both in-run and inside the optimizer step, with exact
  parameter/state inventory, bytes, and source identity;
- preserve persistent shadow-grad/direct-copy behavior and the exact
  update-equivalence grammar (finite/max-absolute/max-relative/cosine,
  update-norm, scalar-step, and unchanged-input checks);
- compare matched controls and candidate arms at identical initialization,
  data order, schedule, precision, and tuning allowance; preserve negative,
  deletion, and rollback outcomes;
- require CUDA bit-equivalence and peak-reduction evidence before any current
  mechanism is promoted, with single-tenant and exact current-3B identity
  bindings.

### #898: dispatch, governor, process, and custody owner

The following clauses survive as current Ember Lab custody obligations:

- use the existing Ember Lab dispatch/lease/Windows Job Object and
  operational-receipt authority; no historical launcher, daemon, lease, or
  parallel receipt family;
- bind the exact current 3B source/config/binary identity and declared
  resource profile at admission;
- distinguish conservative launch limits from elastic runtime limits, and
  measure commit/headroom, disk, VRAM, process-tree, and child liveness at the
  governed boundaries;
- retain structured worker/process traces and receipt-before-abort ordering;
  every refusal or owned abort emits a content-addressed, self-diagnosing
  operational receipt before control action;
- enforce pace and whole-run coverage without the old post-return blind window;
  an owned breach may stop only the identity-verified Ember Job Object;
- inventory and refuse foreign/system/WSL/Docker pressure without terminating
  or mutating foreign processes; preserve exact negative, rollback, and
  custody evidence.

## Current architecture and claim boundary

`#707` and `#898` are existing canonical issue owners; the version-controlled
EMBER-02/EMBER-05 contracts and the current Ember Lab governed runner remain
the sole authority. This carrier introduces no launcher, daemon, supervisor,
lease, optimizer, corpus, model, or receipt family. Historical hashes and
measurements are never substituted for current source, config, binary,
checkpoint, or tenancy evidence.

After this document and the two append-only transfer comments are public, the
smallest lossless disposition is to retire only the historical #627 vehicle as
superseded/not planned. #707 and #898 remain open, and all transferred work
still requires fresh current-authority evidence. No GPU, training, checkpoint,
capability, speed, or milestone claim follows from this crosswalk.

`NO_NEW_PARALLEL_AUTHORITY`
