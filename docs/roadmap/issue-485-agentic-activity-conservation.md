# Issue #485 agentic-activity conservation

Status: `SUPERSEDED_NOT_PLANNED`

## Ruling

Issue #485's duplicate historical activity carrier is superseded by canonical
EMBER-03. Rung 1 remains historical implementation and live-fire provenance;
the unexecuted Rung-2 same-window agentic cycle remains an acceptance
obligation and receives no completion credit from this ruling.

## Lossless current-owner transfer

Canonical owner: [EMBER-03/#1117](https://github.com/wordingone/ember/issues/1117).

Accepted transfer: https://github.com/wordingone/ember/issues/1117#issuecomment-5224505523

The accepted transfer preserves:

1. Rung-1 provenance from #491/#574/#598/#635/#597: real receipt,
   filesystem-watch, board, watchdog, and goal sources; persisted/coalesced
   activity ledger; fabricated-attribution refusal; and the public live-fire
   report. The historical report records 140,647 ledger lines, 3265/0 in the
   full suite, 95/0 in the activity subset, and an unrelated pre-existing
   typecheck failure. Those historical results do not prove current universal
   completion.
2. The unexecuted Rung-2 bar: a visible same-window installed Ember CLI
   choose -> act -> verify -> continue cycle, with machine-minted journal and
   receipt bindings for tool/executor calls, outputs, replies, receipt
   confirmations, and state transitions.
3. Exact current source, executable, session, launch, run, job, lease, process,
   telemetry, and frame identity, with stale, replayed, malformed, foreign,
   missing, or fabricated evidence refused.
4. Focus-safe operator input, prompt -> observe -> correct cadence,
   intervention-derived autonomy, responsive success, at least three sessions,
   at least two prompt classes, at least two current-public-build launches, and
   wedge/zombie/error/frame negatives with rollback.
5. The first C13 probe as a future machine-minted receipt obligation; neither
   that probe nor the counted Rung-2 sessions already exists.
6. #303 item 1 as duplicate activity-feed scope while #303 item 2 remains a
   distinct bottom-region-sizing obligation.

Accepted canonical substrate remains the #1117 transfers for field parity
(#12), installed graph/operator geometry (#894), live telemetry (#140),
operator sessions (#154), and durable automation/journal behavior (#200).
Those contracts conserve the evidence bar; they do not execute Rung 2.

## Architecture and claim boundary

The current Ember Lab, Ember CLI, goal-mode, activity journal, telemetry, and
receipt/custody spine remain the sole authority. This ruling creates no second
feed, launcher, daemon, loop, board, or receipt family.

`NO_NEW_PARALLEL_AUTHORITY`

No live Rung-2 session, C13 probe, model capability, training, checkpoint,
sufficient-pretraining, or milestone-completion credit is claimed.

## Rollback

Revert this conservation document and reopen #485 if the accepted public
transfer is not posted, is rejected, is removed, is narrowed, or becomes
detached from the canonical EMBER-03 certificate.
