# Issue #739 ordering-apparatus conservation ruling

Status: `SUPERSEDED_NOT_PLANNED` for the standalone old apparatus tracker,
conditional on accepted transfer to EMBER-02/#1116.

Source master: `87f9669c537715ff0008080c22002eff04040bba`.

Review packet SHA-256:
`4B58EA388CB411875697B0537200017BBB76521C05063670008C620CC6C9A6B0`.

## Canonical owner transfer placeholders

Canonical owner: EMBER-02/#1116. Publish with scientific screen #711.

- `[PENDING_ACCEPTED_739_TRANSFER_ON_1116]`
- `[PENDING_BIDIRECTIONAL_LINK_FROM_739]`
- `[PENDING_VERSION_CONTROLLED_CARRIER_MERGE]`

## Historical-only evidence boundary

Existing interval, manifest, permutation, scorer, verdict, and sensitivity
code is apparatus history. The calibration receipt records
`trained_at_all.passed=false` and `floor=null`; fail-closed preflight is the
correct current outcome and grants no screen result.

## Lossless surviving contract

- Keep interval, reservation, manifest, permutation, scorer, verdict, and
  sensitivity components separate and trace each to the frozen prereg clause.
- Reproduce production loader geometry, final-short-window handling,
  exclusion-before-trim, exact batch multiple, and refusal to wrap.
- Require identity permutation to reproduce the sequential consumer and every
  arm to retain the same full-example hash multiset.
- Preserve scorer round-trip, IQR, tie-block, canonical-correlation, and
  opposite-order sanity kills.
- Preserve the frozen sign convention, per-cell grammar, censoring, precedence,
  and total `5^3` exhaustive verdict mapping with invalid-first behavior.
- Keep sensitivity preflight hard-stopped while the floor is null.
- A successor uses a deliberately learnable structured or repeated target,
  same multiset, exact consumer, direction and floor frozen before execution,
  a different production seed, and trained-at-all PASS.

## Exact falsifier and reopen rule

No new learnable calibration, non-null frozen floor, and exact-consumer
production sensitivity receipt means the apparatus cannot authorize the main
ordering screen. A structural unit test cannot substitute for this evidence.

## Credit boundary

- `completion_credit=false`
- `scientific_execution_credit=false`
- `acquisition_credit=false`
- `result_credit=false`
- `gpu_credit=false`
- `training_credit=false`
- `checkpoint_credit=false`
- `capability_credit=false`
- `milestone_credit=false`

Current Ember Lab, EMBER-02, and the existing training/corpus/receipt spine
remain the sole authorities.

`NO_NEW_PARALLEL_AUTHORITY`
