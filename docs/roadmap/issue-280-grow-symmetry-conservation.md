# Issue #280 terminal conservation ruling

Status: `SUPERSEDED_NOT_PLANNED` for the historical rung-1 cbase
718M-shell / `ff8192 -> ff16384` exact-duplicate grow vehicle. The surviving
current-scale differentiation obligations are conserved by canonical EMBER-05
issue #1119, with EMBER-02 issue #1116 retaining the foundation-scale context.

Source master: `87f9669c537715ff0008080c22002eff04040bba`.

## Authority and credit boundary

- `completion_credit=false`
- `result_credit=false`
- `gpu_credit=false`
- `training_credit=false`
- `checkpoint_credit=false`
- `capability_credit=false`
- `milestone_credit=false`

The historical finding remains `PROVISIONAL` because its adversary pass was
pending. The operational gate nevertheless applies immediately: no growth arm
may launch with an exact-duplicate operator known to preserve twin symmetry.
This ruling does not upgrade the old finding or claim that a current 3B grow
has executed.

## Historical evidence retained

The rung-1 grow duplicated units exactly and left every paired unit
bit-identical after stabilization. Median twin cosine was effectively one and
median relative L2 was exactly zero while non-duplicated parameters moved.
That is historical evidence that the named grow was capacity-null, not proof
about a current Ember growth operator. The stable-rank output from the audit
was defective and remains unusable.

The original launch gate and repair contract remain intact:

1. Persist a genuine pre-grow checkpoint, optimizer state, RNG state,
   manifest, and exact hashes before widening.
2. Break symmetry with an antisymmetric down-projection split
   `(a/2 + eps, a/2 - eps)` so the pair still sums to `a` in real arithmetic.
3. Register the perturbation distribution and per-column scale relative to
   column norm before execution, with a second scale as a sensitivity cell.
4. Derive the expected twin-divergence acceptance band from the registered
   sigma before observing the run.
5. At step zero, require function preservation under a frozen cross-width BF16
   reduction-order tolerance. Bit equality is neither required nor accepted as
   the tolerance rule.
6. After stabilization, load the persisted weights and prove every split pair
   leaves the capacity-null band; receipt the production optimizer's exact
   grow handling.
7. Use segment-scoped checkpoint directories, absolute artifact identities and
   SHA-256 bindings, and record the learning-rate schedule position.

## Lossless transfer

Canonical transfer URL placeholders, to be replaced only by accepted
append-only public comments:

- EMBER-05 / #1119: https://github.com/wordingone/ember/issues/1119#issuecomment-5224550589
- EMBER-02 / #1116: https://github.com/wordingone/ember/issues/1116#issuecomment-5224550408

#1119 owns differentiated useful capacity, persistence, matched controls,
deletion/rollback, and the current 3B-or-larger growth campaign. #1116 owns the
minimum-3B birth, forbids exact expert twins, and preserves exact-duplicate
growth as negative starting evidence. Neither owner inherits a result from the
historical cbase run.

## Reopen and falsifier

Reopen this disposition if either accepted transfer omits the provisional
status, immediate no-exact-duplicate launch gate, antisymmetric mechanism,
registered sigma and derived bands, cross-width BF16 step-zero tolerance,
genuine pre-grow custody, post-stabilization loaded-weight audit, or
negative/rollback evidence. A current claim is falsified by any unbound twin,
missing pre-grow state, exact-duplicate operator, post-hoc band, or function
preservation outside the frozen tolerance.

`NO_NEW_PARALLEL_AUTHORITY`
