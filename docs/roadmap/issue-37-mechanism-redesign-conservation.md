# Issue #37 mechanism-redesign ladder conservation

Status: `SUPERSEDED_NOT_PLANNED` only after this exact carrier receives an independent exact-head PASS, fresh required checks are green, and the carrier merges. This document does not claim that L1, L2, or L3 executed on the current owned >=3B lineage.

## Historical subject retired

Issue #37 recorded a frozen response to A20 fire-4. On the grown 1.222B step-766 substrate, all 16 checkpoint evaluations and the final battery recorded train 2/5 and heldout 0/3, engaging the historical v2.2-shape GPU ban. The later #41 dossier correction remains binding: the result did not disconfirm trainable substrate capacity; it disconfirmed a larger, better but frozen representation feeding the same small head-only LoRA adapter.

The historical checkpoint SHA `58e8e989...`, cbase/timeshare runner, `EMBER_GATE_AUTHORIZED`, legacy launch variables, and sub-3B execution vehicle remain provenance only. They are not revived as current authority.

## Accepted current owner

OPEN EMBER-05/#1119 accepts the complete surviving L1 -> L2 -> L3 scientific and evaluation contract:

https://github.com/wordingone/ember/issues/1119#issuecomment-5225407258

The bidirectional #37 bridge is:

https://github.com/wordingone/ember/issues/37#issuecomment-5225408428

OPEN EMBER-02/#1116 remains prerequisite context for the sufficiently trained current owned 3B foundation but receives no #37 scientific obligation. #41 remains historical dossier and caveat provenance only. #1119 stays open after this carrier.

## L1 — adapter locus

On the first admissible current owned >=3B substrate, compare head-only adaptation with the same rank budget placed on interior mid-depth FF up/down projections, leaving the head untouched in the treatment.

Run the same TinyPolicyTransformer-shaped CPU toy first for three frozen seeds. A governed GPU window is admissible only if the toy shape differs from head-only. The exact kill remains: heldout is 0/3 for all three seeds while train acquisition is unchanged.

Freeze and receipt the current-scale adaptation surface, layer/module inventory, rank, initialization, data, seeds, source, checkpoint, evaluator, active parameter, and trainable parameter identities.

## L2 — objective

Preserve the heldout firewall. On train states only, add a transfer-pressure term that rewards an answer only when the same adapter state answers a train state whose exemplar context omits that state. Anneal the term from zero to the preregistered lambda during the first 256 steps.

Refuse or kill the arm if degenerate resampling exceeds three times A19's historical rate or train acquisition does not reach 2/5 by step 320. Bind the exact reward definition, annealing schedule, resample counter, train-state and exemplar identities, and proof that heldout data never enters training or reward.

## L3 — presentation

Freeze a k=3 to k=0 exemplar schedule before execution, using the current corpus generator's deterministic knobs rather than adding a second mechanism. Evaluation and the heldout firewall remain unchanged.

Kill the arm if train acquisition reaches 0/5 at k=0 and does not recover by step 768. Bind each schedule transition, context construction, train/eval identity, and receipt.

## Sequence and terminal rule

Execute L1 -> L2 -> L3, one intervention per window. Each later arm requires the prior arm's independently reopenable verdict. Do not combine arms until one alone produces heldout >0.

If all three arms exhaust with heldout still zero, preserve the complete negative trail and escalate the corpus/task-structure hypothesis class to the operator. Do not silently widen scope, combine unearned mechanisms, or erase the mechanism family.

## Current authority translation

Any future execution must use one exact owned >=3B checkpoint and the existing Ember Lab governed dispatch, lease, process, resource, receipt, evaluation, and custody spine. Freeze the matched substrate, data, seeds, envelope, active/trainable parameter counts, optimizer, stopping, and evaluator for every comparison.

The historical single-variable intent survives without reviving its model, launcher, daemon, ledger, receipt family, or execution authority. Current Ember Lab and the existing EMBER-05 execution/evaluation/custody spine remain sole authority.

## Refusal, negatives, and rollback

- Missing, foreign, stale, mutated, self-attested, partial, reordered, combined-arm, heldout-leaking, or identity-incomplete evidence refuses admission.
- Receipt-before-abort, interruption, deletion, rollback, resource refusal, and unrelated-regression evidence remain mandatory.
- A negative result constrains only the exact tested locus, objective, and presentation regime.
- Historical checkpoint bytes, prose, a toy alone, or a partial current run cannot become scientific completion by citation.
- Rollback is ordinary Git revert of this carrier plus reopening #37 while accepted owner evidence remains append-only custody.

## Terminal falsifier and reopen rule

Scientific completion requires a current-master independently replayable receipt set binding the ordered L1 -> L2 -> L3 ladder, each CPU falsifier and admitted GPU decision, the unchanged heldout firewall, matched identities, and the terminal verdict.

This carrier permits only historical-vehicle supersession. Loss or narrowing of the accepted #1119 transfer, the #37 bridge, A20 result, #41 caveat, exact L1/L2/L3 gates, sequence rule, heldout firewall, all-negative escalation, current-authority translation, refusal/negative/rollback law, or credit boundary reopens #37.

## Credit and authority boundary

- `completion_credit=false`
- `scientific_execution_credit=false`
- `acquisition_credit=false`
- `result_credit=false`
- `gpu_credit=false`
- `training_credit=false`
- `checkpoint_credit=false`
- `capability_credit=false`
- `milestone_credit=false`

OPEN #1119 remains the sole current owner for this ladder. No evidence is inherited from unrelated issues or historical runs.

`NO_NEW_PARALLEL_AUTHORITY`
