# Issue #711 curriculum-ordering conservation ruling

Status: `SUPERSEDED_NOT_PLANNED` for the old sub-3B/shards-v1-stream run
carrier, conditional on accepted transfer to EMBER-02/#1116.

Source master: `87f9669c537715ff0008080c22002eff04040bba`.

Review packet SHA-256:
`4B58EA388CB411875697B0537200017BBB76521C05063670008C620CC6C9A6B0`.

## Canonical owner transfer placeholders

Canonical owner: EMBER-02/#1116. Publish with apparatus tracker #739.

- `[PENDING_ACCEPTED_711_TRANSFER_ON_1116]`
- `[PENDING_BIDIRECTIONAL_LINK_FROM_711]`
- `[PENDING_VERSION_CONTROLLED_CARRIER_MERGE]`

## Historical-only retirement

The old smaller-network subject and shards-v1-stream identity are historical
apparatus evidence. Current neural research runs only at the admitted 3B scale.
The three failed sensitivity calibrations are disclosed `INVALID_SCREEN`
evidence, not a null or positive curriculum result.

## Lossless surviving contract

- Every arm consumes the exact same token multiset once with matched
  initialization, total tokens, optimizer, tuning budget, and evaluation.
- Preserve three arms: random order, low-to-high PPM surprisal, and high-to-low
  directional control.
- Use paired seeds and judge heldout fixed-token curves plus
  compute-to-frozen-target; include the PPM scorer bill in training cost.
- Derive per-source token intervals before selecting scorer reservations or
  building any training manifest.
- Exclude every window intersecting a reserved scorer interval before
  canonical batch trim.
- Require range-level and example-level zero intersection, H1 disjointness,
  exact corpus identity, and boundary probes.
- Run a same-multiset, exact-consumer, known-direction positive control before
  the main screen.
- Preserve the failed calibration history: the frozen floor is absent because
  `trained_at_all` failed; no fourth silent tune or post-hoc floor is allowed.
- A successor positive control must plant a deliberately learnable repeated or
  structured target, retain the same multiset and consumer path, and freeze
  its direction and floor before execution.

## Exact falsifier and reopen rule

Without a trained-at-all calibration PASS, non-null frozen floor, and a
production-seed sensitivity receipt from the exact consumer, the main ordering
screen is prohibited. A failed apparatus does not kill or confirm curriculum
ordering; it keeps EMBER-02's data-ordering obligation open.

## Credit boundary

- `completion_credit=false`
- `scientific_execution_credit=false`
- `acquisition_credit=false`
- `result_credit=false`
- `gpu_credit=false`
- `training_credit=false`
- `checkpoint_credit=false`
- `capability_credit=false`
- `milestone_credit=false`

Current Ember Lab, EMBER-02, and the existing corpus/training/evaluation
custody spine remain the sole authorities.

`NO_NEW_PARALLEL_AUTHORITY`
