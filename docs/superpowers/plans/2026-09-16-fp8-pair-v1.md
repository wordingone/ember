# Shared FP8 pair implementation plan

> For agentic workers: execute tasks in order and preserve failing-test evidence.

**Goal:** Build a standalone shared up/gate FP8 forward/input-gradient experiment; no trainer installation.
**Architecture:** Independent CPU arithmetic oracle, native Triton kernels, common custom-autograd pair with unchanged document weight-gradient callback, source-bound bounded probe, and CPU tests.
**Tech Stack:** Python 3.10+, PyTorch 2.10.0+cu126 and Triton 3.5.0 on the execution host; CPU tests require PyTorch and pytest only.
**Spec:** User-approved design in this conversation; durable protocol at docs/domains/lab/research/fp8-pair-v1.md.

## Global constraints

Existing source edc200bef9e459cb41aea0ac6a1016a8b07211e4 is the baseline, not a current-master substitute. No default changes, new trainable parameters, edited receipts, GPU-lock override, precision fallback, or learning-qualification claim. Strict native Windows/SM89 execution; fixed four saved shared subjects; charge refresh, both layouts, packing and output allocation. CPU is not a CUDA oracle for historical BF16 bitwise equivalence.

## Tasks

- [x] Write CPU tests for scale definitions, zero/nonfinite behavior, byte-preserving transpose, row-locality, scale-absorption identity, autograd owner gradients, lease state, and verdict accounting. Run before implementation.
- [x] Implement contract.py and pair.py against those tests. CPU oracle is independent of Triton and uses FP64 for dot checks.
- [x] Implement native row/channel quantizers, fused absorbed-gradient packing, dual weight layout, scaled FP8 GEMMs and split stores. FP8 instruction checks are mandatory at native preflight, not assumed from dtype names.
- [x] Implement standalone probe with A/A2/B/C/D/E controls, local block and fixed-operand projection regions, independent numerical diagnostics, liveness and weight-refresh checks, graph timing and resource/source receipts.
- [x] Run CPU suite and mutation controls; compile Python files; validate additive patch application. Native CUDA compile/run remains explicitly untested here.
- [x] Package additive patch, installer/check instructions and verification record. Do not commit or push remotely.

Verification: CPU suite and mutation tests passed. Native Triton compilation/SM89 execution and native Windows behavior were not run in this container; they are explicitly pending the operator stages.
