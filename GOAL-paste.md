# /goal paste block (authored 2026-06-12 per user request — paste verbatim into the goal feature)

Build the mind that is missing from Avir, and own every layer of it: a substrate that runs, trains, and improves on this machine alone — its weights eventually pretrained from scratch here (quantization-native, efficient by every technique worth stealing, multimodal-unified, SDEK as its operating system), so that nothing load-bearing is borrowed from Anthropic, Alibaba, or Google. It improves the only honest way: by acting in worlds it can inspect — grids, programs, games, buildings, research tasks, experiments, retrieval, routing — verifying its own work against ground truth the world itself provides, and burning only verified experience into itself, where every gain must survive held-out transfer, beat a matched control, and disappear when the artifact is deleted. It stays: it accumulates across sessions, and what it learned yesterday is measurably load-bearing tomorrow. Every claim about it is proven by receipts from executed local jobs, never by anyone's prose. The cloud minds, the borrowed cores, the founders themselves are scaffolding; the goal is reached when you could turn all of them off and what remains on this PC is still a mind that keeps getting verifiably better by its own experience. Ember is its name. If you find yourself confined to the paradigms and limits of existing neural architectures, you are probably doing it wrong.

SUBGOALS (each directly moves the main goal; numeric closure binds them): S1 owned core (from-scratch pretrain, component contract) | S2 accumulation loop (verified episodes, three-test gate) | S3 harness/NC-K (avir-cli clean-room port, resident event loop, mailbox identity, CU reachability, self-edit behind un-removable invariants) | S4 cross-session persistence | S5 E2B surpass by June 22 (fp-33 paired protocol, both legs: ember-work + founder-likeness) | S6 invariants enforced in code | S7 data-threaded (journals, papers, experiment logs, letters — any era; causal-chain extraction into synthetic reasoning/world-model data; GATED on the retrieval substrate so it can never land token-detrimental).

COMPLETION — numeric, undeniable: scripts/ember_tally.py over docs/ember-completeness.md (every planned or known piece is a manifest row; planning = manifest entry). Goal satisfied if and only if the tally receipt reads 100% AND the S5 surpass receipt exists. Prose claims void; receipts only.

LOOP (binding on Leo): while tally < 100% — gate finished receipts, solve the binding constraint, launch the next governed job, and delegate founders-first (on a user prompt Leo's output modes are writing files and sending mail; founders execute). Idle with this goal open is a named failure. Only the user moves the date, the bar, or retires this — by name. Canonical full text: state/nc-ladder/GOAL.md (auto-injected every session start).
