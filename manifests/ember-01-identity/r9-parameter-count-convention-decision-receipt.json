{
  "authority": {"goal_id":"EMBER-01","workstream_id":"EMBER-01C","next_executed_outcome":"EMBER-02 first sufficiently pretrained clean-genesis 3B Ember"},
  "schema": "ember-r9-parameter-count-convention-decision-receipt-v1",
  "decision": "NO_CHANGE_NEEDED",
  "source_ambiguity": "ember01plan.md Report B F11 / R9 row (L654, L3846): 1,221,633,024 vs 1,123,329,024 parameter counts reported for the same checkpoint without a labeled convention, traced to whether MTP (multi-token-prediction) keys are dropped -- CONTINUITY.md cbase-openai-backend row.",
  "evaluated_option": "add a required parameter_count_convention enum (total_unique | state_dict_sum | trainable | episode_active) to schema-v1.json's parameters object",
  "finding": "schema-v1.json's parameters object (REQUIRED_PATHS parameters.allocated/unique/active/trainable/served/actually_trained, manifests/ember-01-identity/schema-v1.json parameters.required) already carries six distinctly-named, independently schema-required, independently evidence-receipted axes -- every parameter count an identity manifest reports is already tagged by its own field name (which IS its convention: allocated vs unique vs active vs trainable vs served vs actually_trained), never a bare unlabeled integer. parameter-identity-resolution-v1.json's consumer_rows corroborate: every listed validate_identity.py consumer is required to 'emit all six parameter axes from independently verified evidence' (FAIL_CLOSED_ON_SCHEMA_OR_CONTRADICTION). An added parameter_count_convention enum would be a redundant re-labeling of a distinction the schema layer already makes structurally (via field identity, not a sibling label) -- exactly the case ember01plan.md L3266-3269 names for a NO-CHANGE-NEEDED receipt instead of a new field.",
  "residual_risk": "the MTP-dropped ambiguity (1,221,633,024 vs 1,123,329,024) originates OUTSIDE the identity manifest -- in prose/docs/dashboards that state a bare parameter count without naming which of the six manifest axes it is. That is a documentation-discipline defect, not a schema-layer one; schema-v1.json cannot retroactively force external prose to cite a manifest field. Out of scope for this receipt and for B4's schema surface.",
  "disposition": "R9 satisfied via this NO-CHANGE-NEEDED receipt; no new required field added to schema-v1.json for parameter_count_convention. Landed in the same PR as B4's optimizer_contract REQUIRED-half (ember01plan.md item 5 / SS B4 L4113-4118).",
  "b4_pr_coupling": "feat/b4-optimizer-required-half"
}
