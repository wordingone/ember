# Contract C6/C7: CLI And Goal Mode

Status: DRAFT.

## Uncheatable Form

Build or run Ember CLI and goal mode `X` that beats ordinary checklist/chat workflow `Y` on governed-run reproducibility and premature-completion rejection metric `Z` by threshold `T`, preserving equal task/evidence/time constraints `C`, under budget `B`, verified by protocol `V`, producing PASS, FAIL, or INVALID-RUN.

## Required Failure Case

At least one intentionally failed or interrupted run must be handled correctly. Happy-path success is invalid.

## Current Verdict

NOT RUN.
