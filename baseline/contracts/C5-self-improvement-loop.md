# Contract C5: Self-Improvement Loop

Status: DRAFT.

## Uncheatable Form

Build or run Ember loop `X` that beats baseline loop `Y` on external ML task set `Z` by threshold `T`, preserving equal tool/data/compute/time/human constraints `C`, under budget `B`, verified by scorer or reviewer protocol `V`, producing PASS, FAIL, or INVALID-RUN.

## Current Verdict

INVALID-RUN if attempted now. The task subset, baseline loop, and scorer are not locked.
